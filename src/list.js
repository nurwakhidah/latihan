// import React, { Component } from "react";

// const List = ({layout, products}) => (
//   <ul className = {layout}>
//     {products.map((product, index) => {
//       return (
//         <li>
//           {
//             layout === "list" ? <p>{product.name}</p> : ""
//           }

//           {
//             layout === "list" ? <p>{product.price}</p> : ""
//           }
//         </li>
//       );
//     })}
//   </ul>
// );

// export default List;